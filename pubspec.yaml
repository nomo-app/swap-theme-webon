name: swap_theme_webon
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  flutter_web_plugins:
    sdk: flutter
  nomo_ui_kit:
        path: packages/nomo-ui-kit
  nomo_router:
    path: packages/nomo-ui-kit/packages/nomo-router
  route_gen:
    path: packages/nomo-ui-kit/packages/nomo-router/route_gen
  webon_kit_dart:
        path: packages/webon-kit-dart

  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  flex_color_picker: ^3.3.1
  logger: ^1.4.0
  hooks_riverpod: ^2.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.8
  flutter_lints: ^2.0.0


flutter:
  uses-material-design: true
